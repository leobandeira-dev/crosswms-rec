
import { Router } from 'wouter';
import { SimpleRouter } from '@/components/SimpleRouter';

const AppRoutes = () => {
  return (
    <Router>
      <SimpleRouter />
    </Router>
  );
};

export default AppRoutes;
